#include <stdio.h>
#include <math.h>

int main() {
  float x;
  x = sin(30);
  printf("sin(30 degree) = %f\n", x);

  x = sqrt(2);
  printf("sqrt(2) = %f\n", x);

  x = log(10);
  printf("log(10) = %f\n", x);

  return 0;
}